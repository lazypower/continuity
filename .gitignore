# Binary
/continuity
/continuity-*

# Go
*.exe
*.test
*.out
vendor/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Build artifacts
dist/
ui/node_modules/
ui/dist/
cmd/continuity/ui/

# Data (never commit user data)
*.db
*.db-shm
*.db-wal

# Exception: migration golden fixtures are deliberately committed. They are
# tiny, seeded images minted from real released binaries (not user data) and
# feed the hermetic migration regression test. See scripts/gen-migration-fixtures.sh.
!internal/store/testdata/migration/**/*.db
